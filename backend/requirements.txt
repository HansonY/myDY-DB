# ⚠️ f2 把依赖版本钉死(httpx==0.27.2 / click==8.1.7 / cryptography==44.0.0 等),
#    因此本文件不再对这些传递依赖加约束,一律让 f2 的钉子生效,避免 ResolutionImpossible。
#    若将来 f2 的钉子与后端冲突到无法共存,再把 f2 拆到独立 venv 用 CLI 子进程调用。

# 采集
f2>=0.0.1.7            # 抖音/TikTok 多平台异步采集(收藏/喜欢列表)
yt-dlp>=2025.1.1       # 兜底:单链接补抓 + 取字幕

# 后端
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic-settings>=2.7
python-dotenv>=1.0

# MCP 服务(让 AI 直接查库 / 触发采集)
# 上界必须钉死。f2 要求 pydantic==2.9.*,而:
#   mcp 2.x   → 需要 pydantic>=2.12
#   mcp 1.13+ → 需要 pydantic>=2.11
# 都和 f2 冲突。只有 mcp 1.12.x 能与 pydantic 2.9 共存,`pip check` 才干净。
# 踩过两次坑:
#   ① 只写 `mcp>=1.2`:我的环境解析到 2.0、干净克隆解析到 1.12.4,
#      同一份代码一边能跑一边 ModuleNotFoundError(2.x API 完全不同)。
#   ② 放宽到 `<2`:又会解析到 1.29,再次和 f2 的 pydantic 钉子冲突。
mcp>=1.9,<1.13
